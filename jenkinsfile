pipeline {
  agent any
  tools {
    nodejs 'nodejs'
  }
  stages {
    stage("build") {
      steps {
        echo "🚀 Building the application"
        sh 'docker build -t static .'
      }
    }

    stage("deploy") {
      steps { 
        echo '🚀 Deploying the application'
        sh 'docker start static . || docker run --name static_jenkins -d -p 81:80 static .'
        sh 'docker ps'
        echo "🚀🚀🚀"
      }
    }
  }
}
